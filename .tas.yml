framework: jest
preRun:
  command:
    - yarn install
postMerge:
  pattern:
    - "./test/cleverMerge.unittest.js"
preMerge:
  pattern:
    - "./test/cleverMerge.unittest.js"
postRun:
  command:
    - yarn --version
version: 1.0
